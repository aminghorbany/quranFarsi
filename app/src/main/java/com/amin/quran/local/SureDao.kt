package com.example.panel.local.db

import androidx.room.*
import com.amin.quran.models.SureModel

@Dao
interface SureDao {

//    @Insert(onConflict = OnConflictStrategy.REPLACE)
//    suspend fun insertOrderInfo(data : SureModel)

}